# Personal identity lives in git/.gitconfig.local (gitignored).
# Copy git/.gitconfig.local.example → git/.gitconfig.local and fill in your details.
[include]
	path = ~/.dotfiles/git/.gitconfig.local

[core]
	editor = vim
	autocrlf = input
[init]
	defaultBranch = main
[pull]
	rebase = true
[push]
	autoSetupRemote = true
[alias]
	st = status
	co = checkout
	br = branch
	ci = commit
	unstage = reset HEAD --
	last = log -1 HEAD
	visual = !gitk
